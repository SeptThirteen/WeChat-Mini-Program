// Change this value to point at your backend server.
// Use localhost when the backend runs on the same Windows machine in DevTools.
const BASE_URL = 'http://127.0.0.1:8080';

export default BASE_URL;
